# Set to "true" to run with no backend/AI calls (browser-only demo).
# Defaults to true in production builds, false in local dev.
VITE_DEMO_MODE="false"

# Your own Supabase project (needed when VITE_DEMO_MODE is false)
VITE_SUPABASE_URL=""
VITE_SUPABASE_PUBLISHABLE_KEY=""
VITE_SUPABASE_PROJECT_ID=""
